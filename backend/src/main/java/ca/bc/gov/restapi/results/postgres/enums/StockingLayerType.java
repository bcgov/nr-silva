package ca.bc.gov.restapi.results.postgres.enums;

/** Whether a Stocking Standard's Tree Stocking Criteria uses a single layer or four layers. */
public enum StockingLayerType {
  SINGLE,
  MULTI;
}
